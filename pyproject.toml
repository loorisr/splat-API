[project]
name = "splat-api"
version = "1.0"
description = "RF propagation API using signalserver/splat! as a backend"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.136.1",
    "pydantic>=2.13.3",
    "httpx>=0.28.1",
    "uvicorn>=0.46.0",
]
